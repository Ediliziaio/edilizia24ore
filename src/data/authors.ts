/**
 * Editorial identity.
 *
 * Articles are signed by the newsroom, not by named individuals. Naming
 * authors that cannot be verified is worse for E-E-A-T than an honest
 * collective byline, especially on YMYL topics such as tax deductions —
 * so there are no author personas and no /autore/ pages.
 * Accountability lives on /chi-siamo (who publishes, how) and /metodologia
 * (how the comparisons are built).
 */

export const EDITORIAL_BYLINE = 'Redazione Edilizia 24 Ore';

/** Where readers can check who is behind the content. */
export const EDITORIAL_POLICY_PATH = '/chi-siamo';
export const METHODOLOGY_PATH = '/metodologia';
